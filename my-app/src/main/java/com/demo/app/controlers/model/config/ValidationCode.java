package com.demo.app.controlers.model.config;

public enum ValidationCode {
    OK,
    VALID,
    USER_ERROR,
    ACCOUNT_ERROR,
    TRANSFER_ERROR,
    USER_TRANSFER_ERROR,
    PAYMENT_TRANSFER_ERROR,
    EXTERNAL_ERROR,
    SAVING_ERROR
}
